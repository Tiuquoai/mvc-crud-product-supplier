<%- include('../partials/header') %>
<h2>Edit Product</h2>
<form action="/products/<%= product._id %>/edit" method="POST">
  <label>Name:</label><br>
  <input type="text" name="name" value="<%= product.name %>" required><br><br>

  <label>Address:</label><br>
  <input type="text" name="address" value="<%= product.address %>" required><br><br>

  <label>Phone:</label><br>
  <input type="text" name="phone" value="<%= product.phone %>" required><br><br>

  <label>Supplier:</label><br>
  <select name="supplierId" required>
    <% suppliers.forEach(s => { %>
      <option value="<%= s._id %>" <%= product.supplierId && product.supplierId.equals(s._id) ? 'selected' : '' %>>
        <%= s.name %>
      </option>
    <% }) %>
  </select><br><br>

  <button type="submit">Update</button>
</form>
<%- include('../partials/footer') %>
